# `evaluations/`

First-class evaluation harness assets (built in S8).

- `datasets/` — versioned golden scenarios (≥50 synthetic cases).
- `reports/`  — generated evaluation reports and metric snapshots.
