# `app/providers`

**Planned stage:** S4

LLM provider abstraction with routing and fallback: deterministic mock (tests/CI) -> Ollama (local) -> optional hosted provider.

> Intentionally empty in S0 (Foundations). No business logic is implemented here yet; this package exists to fix the project's separation of concerns.
