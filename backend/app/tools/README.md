# `app/tools`

**Planned stage:** S2

Registry of strictly-typed, least-privilege agent tools. Read-only tools are model-facing; write/execute tools run only via the post-approval outbox executor.

> Intentionally empty in S0 (Foundations). No business logic is implemented here yet; this package exists to fix the project's separation of concerns.
