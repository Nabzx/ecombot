# `app/rules`

**Planned stage:** S2

Deterministic business-rules engine (return/refund/cancellation eligibility, limits, ownership, duplicates). Pure functions, unit-tested, authoritative over the LLM.

> Intentionally empty in S0 (Foundations). No business logic is implemented here yet; this package exists to fix the project's separation of concerns.
