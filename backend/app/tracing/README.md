# `app/tracing`

**Planned stage:** S7

Custom end-to-end traces stored in PostgreSQL (steps, tool calls, model calls, latency, tokens, cost) with an optional OpenTelemetry exporter.

> Intentionally empty in S0 (Foundations). No business logic is implemented here yet; this package exists to fix the project's separation of concerns.
