# `data/`

Synthetic-only datasets for AgentOps. Populated from S1 onward.

- `synthetic/` — generated customers, orders, shipments, tickets (seed output).
- `policies/`  — authored company policy corpus (source for RAG in S3).

No real or private company data is ever stored here.
